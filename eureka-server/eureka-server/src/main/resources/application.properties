spring.application.name=eureka-server
# 1. Set the default port for Eureka Server
server.port=8761

# 2. Configure Eureka Client properties
# The server is not a client, so it should not register or fetch the registry
eureka.client.register-with-eureka=false
eureka.client.fetch-registry=false